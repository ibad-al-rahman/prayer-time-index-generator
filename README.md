# Prayer Time Index Generator

A tool to index prayre times and publish them as static files instead of rely on a server and database for such a small task

## Usage

```
ptig v1 -y 2024 -i ./input -o output
```
